#include <iostream>
#include <string>

int main() {

  // a string variable
  std::string name = "Zenva";

  /* a string can be any combination of letters, numbers, symbols, etc. as long as it's between "" */
  std::string mixedString = "1n. ";
  std::cout << mixedString << std::endl;

  /* a numerical representation of a string is not the same as the number, and a string of length 1 is different from a character */
  // int numOne = 1;
  // char one = '1';
  std::string stringOne = "1";
}
